# is become_method overridden globally?
grep -rn 'become_method\|become_exe\|become_flags' ansible.cfg group_vars/ inventories/ 2>/dev/null
cat ansible.cfg | grep -iA3 'become'

# does prod use a different inventory/cred where appadmintemp CAN sudo-runas solace?
#   (your NEW VMs have temp creds + Centrify; prod VMs may have a sudoers rule)
