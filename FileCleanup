# the real proof — SEMP answers
curl -s -u admin:admin http://localhost:8080/SEMP/v2/monitor/about/api | grep -o 'responseCode.:.200'

# POST passed, broker fully booted (not just container Up)
podman logs solace 2>&1 | grep -iE 'platform type|pre-startup'

# and the stability test — still up in a few minutes = loop is truly dead
sleep 120 && systemctl --user is-active solace.service && podman ps --format '{{.Names}} {{.Status}}'
