# how does the podman-run path set the container user?
grep -rn 'user\|PSP_USER_ID\|id -u\|--user' roles/solace_systemd/ roles/solace_runtime_deploy/ 2>/dev/null | grep -iv '#'


podman inspect solace --format '{{.Config.User}}'   # the actual UID the prod container runs as
