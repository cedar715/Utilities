cd /home/solace/solace
systemctl --user stop solace.service

# park both confusing VM2 files out of the way
mv solace-compose.yml solace-compose.yml.ghcp-bad 2>/dev/null
mv /home/solace/solace-compose.yml /home/solace/solace-compose.yml.homedir-bak 2>/dev/null

# create the live file fresh from VM1's content
vi /home/solace/solace/solace-compose.yml
#   paste VM1's content, then change ONLY:  hostname: solace1  ->  hostname: solace2
