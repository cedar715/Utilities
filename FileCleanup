# what Type and notify settings is the RUNNING unit using on VM2?
systemctl --user cat solace.service | grep -iE 'Type=|NotifyAccess|sdnotify|ExecStart|TimeoutStartSec'

# on VM1 (the stable one) — what does ITS unit say?
systemctl --user cat solace.service | grep -iE 'Type=|ExecStart'
