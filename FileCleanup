# what's listening on 2222?
ss -tlnp 2>/dev/null | grep ':2222' || sudo ss -tlnp | grep ':2222'

# is it a leftover podman/conmon from all the earlier crash-loop attempts?
podman ps -a
ps -ef | grep -iE 'conmon|rootlessport|podman' | grep -v grep
