# are the two files the same, or different?
diff /home/solace/solace-compose.yml /home/solace/solace/solace-compose.yml && echo "IDENTICAL" || echo "DIFFERENT"

# which one has the 1001:0 / opt-apps problem?
echo "=== home-dir copy ==="; grep -iE 'user:|device:|storage-group|solace_storage' /home/solace/solace-compose.yml
echo "=== subdir (live) copy ==="; grep -iE 'user:|device:|storage-group|solace_storage' /home/solace/solace/solace-compose.yml
