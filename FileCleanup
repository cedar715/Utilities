ss -tlnp 2>/dev/null | grep ':2222' || sudo ss -tlnp | grep ':2222'

# with sudo, you'll see it again
sudo ss -tlnp | grep ':2222'

# or check the service directly
systemctl is-active sshd     # active
