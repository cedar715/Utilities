solace@vm2[DEV][~] $ systemctl --user cat solace.service | grep -iE 'Type=|NotifyAccess|sdnotify|ExecStart|TimeoutStartSec'
Environment=PSP_COMMON_ARGS_SDNOTIFY="--env=process=podman"
TimeoutStartSec=600
# broker_runtime=podman_compose: start via podman-compose (foreground, Type=simple).
Type=simple
ExecStartPre=-/home/solace/.local/bin/podman-compose -f /home/solace/solace/solace-compose.yml down --timeout 60
ExecStart=/home/solace/.local/bin/podman-compose -f /home/solace/solace/solace-compose.yml up
----
#on VM1 (the stable one) — what does ITS unit say?
systemctl --user cat solace.service | grep -iE 'Type=|ExecStart'
solace@vm1[DEV][~] $ systemctl --user cat solace.service | grep -iE 'Type=|ExecStart'
Type=simple
ExecStartPre=-%h/.local/bin/podman-compose -f %h/solace/solace-compose.yml down
ExecStart=%h/.local/bin/podman-compose -f %h/solace/solace-compose.yml up

