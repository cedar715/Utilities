# is it still running (not looping)?
systemctl --user is-active solace.service        # active
podman ps                                         # solace Up, not restarting

# the actual proof — SEMP answers
curl -s -u admin:admin http://localhost:8080/SEMP/v2/monitor/about/api | grep -o 'responseCode.:.200'

# and let it sit — if it's still up in 5 min, the loop is truly broken
sleep 60 && systemctl --user status solace.service | grep -E 'Active:|since'
