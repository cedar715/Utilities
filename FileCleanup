cd /home/solace/solace
systemctl --user stop solace.service

# back up the bad live file
cp solace-compose.yml solace-compose.yml.ghcp-bad

# promote the GOOD design (home-dir) into the live location the unit loads
cp /home/solace/solace-compose.yml /home/solace/solace/solace-compose.yml

# verify the live file now has the good values
grep -iE 'user:|cap_drop|read_only|cgroupns|storage-group|device:' /home/solace/solace/solace-compose.yml
#   want: user "0:0", NO cap_drop, NO read_only, cgroupns private,
#         direct bind /opt/apps/solace/storage-group, NO device: line
