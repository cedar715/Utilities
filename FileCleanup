# 1. VM1 — confirm its exact user + volume line (our source of truth)
#    (run on VM1)
grep -iE 'user:|/var/lib/solace|storage-group' /home/solace/solace/solace-compose.yml

# 2. VM1 — what host UID actually owns its working storage-group?
#    (run on VM1)
podman unshare ls -lan /home/solace/solace/storage-group | head -3
