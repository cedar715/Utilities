# 1. WHERE does the compose file actually bind-mount storage from?
grep -A2 -iE 'volumes|/var/lib/solace|storage' /home/solace/solace/solace-compose.yml

# 2. does that source path exist anywhere? (maybe it's at a DIFFERENT path than VM1)
podman unshare find /home/solace -maxdepth 4 -name 'storage-group' -o -name 'spool' 2>/dev/null
ls -la /home/solace/solace/ 2>/dev/null
