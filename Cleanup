grep -A4 ulimits ~/solace/solace-compose.yml

systemctl --user restart solace.service
sleep 25
podman exec solace sh -c 'ulimit -n -H; ulimit -l'    # want: 1048576, then unlimited
podman logs solace 2>&1 | grep -iE 'POST|platform type|redundancy|Primary Virtual Router|listening'

curl -u admin:admin http://localhost:8080/SEMP/v2/monitor/about/api    # 200
podman ps    # solace shows Up, not gone
