grep -q XDG_RUNTIME_DIR ~/.bashrc || cat >> ~/.bashrc <<'EOF'

# rootless systemd/podman session (linger-based)
export XDG_RUNTIME_DIR=/run/user/$(id -u)
export DBUS_SESSION_BUS_ADDRESS=unix:path=${XDG_RUNTIME_DIR}/bus
EOF
