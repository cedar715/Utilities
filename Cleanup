# 1. is the unit set to come back on reboot? (linger + enabled = survives reboot)
systemctl --user is-enabled solace.service     # want: enabled
loginctl show-user solace -p Linger            # want: Linger=yes

# 2. capture the exact compose that works, so the recipe is verbatim
cat ~/solace/solace-compose.yml
