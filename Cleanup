# A. Does a TRIVIAL container even run executables here? (isolate broker vs platform)
podman run --rm registry.access.redhat.com/ubi9/ubi:latest /bin/echo works
# if your registry blocks that, use the Solace image you already have:
podman run --rm artifactory.global.example.com/gv-images-products/oss/solace-pubsub-enterprise:10.25.0.217 /bin/echo works

# B. The actual question: does relaxing seccomp fix it?
podman run --rm --security-opt seccomp=unconfined \
  artifactory.global.example.com/gv-images-products/oss/solace-pubsub-enterprise:10.25.0.217 /bin/echo works
