# GHCP context bundle — what to commit where, and status

Commit these into the repo BEFORE pasting the prompt, so GHCP reads them as
part of the codebase (it has repo read access; it cannot see the VMs).

## 1. The proven recipe → commit as
```
docs/solace-rootless-compose-runbook.md
```
(this is the file RUNBOOK-node-standalone.md). It is the source of truth for the
compose specifics and the six landmines. GHCP cannot derive this from code —
it's the by-hand knowledge from VM1.

## 2. The HA topology / naming → commit into
```
inventories/nonp/cn/cdc-dev1/hosts
inventories/nonp/cn/cdc-dev1/group_vars/all.yml
inventories/nonp/cn/cdc-dev1/group_vars/ha_servers.yml
inventories/nonp/cn/cdc-dev1/group_vars/ha_primary_node.yml
inventories/nonp/cn/cdc-dev1/group_vars/ha_backup_node.yml
inventories/nonp/cn/cdc-dev1/group_vars/ha_monitor_node.yml
```

## 3. The prompt → paste GHCP-PROMPT-final.txt into Copilot Chat

---

## Status going in — tell GHCP this is the state

DECIDED / PROVEN:
- Rootless + podman-compose + systemd --user — chosen path (not Quadlet, not rootful).
- One node proven green by hand on VM1 (SEMP 200, reboot-safe).
- The six landmines + fixes — documented in the runbook.
- Networking = host (confirmed allowed by the security baseline).
- HA env keys verified against Solace docs; topology in group_vars.

STILL OPEN (tell GHCP to parameterize / leave for later, not block on):
- The `:55443` "replication endpoints" = cross-site DR replication to the DR
  datacenter — a SEPARATE feature from this 3-node local HA group. Out of scope
  for this generation; do not fold into the matelink.
- TLS server certificates — needed only if/when client TLS ports are exposed.
- Registry hostname + image tag — confirm the real values (placeholders in
  group_vars/all.yml).
- Vault values — redundancy pre-shared key (openssl rand -base64 33) and admin
  password.

## After GHCP generates — validate on the VMs (your job, GHCP can't)
Run the generated playbook against VM2 first (NOT all three) — it's your test
target. Confirm SEMP 200 + POST [ OK ] there before touching VM3 / the primary.
That keeps the automation's blast radius to one node until it's proven.
