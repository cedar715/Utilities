# how do the EXISTING (non-compose) roles become/run as solace?
grep -rn 'become_user\|become_method\|runuser\|su - solace\|ansible_become\|remote_user\|systemctl --user' roles/ group_vars/ | grep -iv compose
