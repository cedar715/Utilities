# full broker output, untruncated — this is the one that matters
cd ~/solace
~/.local/bin/podman-compose -f solace-compose.yml up 2>&1 | tee /tmp/solace-boot.log
# let it run until it dies (~15-20s), then Ctrl-C, and paste the last ~40 lines:
tail -40 /tmp/solace-boot.log
