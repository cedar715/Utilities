stat -fc %T /sys/fs/cgroup                                    # want: cgroup2fs
getent passwd solace | cut -d: -f3,6,7                        # uid, home, shell — expect 5011, /home/solace, /bin/bash
cat /etc/subuid | grep solace                                 # note the range start (VM1 was 755360:65536)
ls -laZd /app/solace/podman/graphroot 2>/dev/null || echo "graphroot not at /app — check storage.conf"
