# did the children get relabeled, or just the top dir?
ls -laZ /app/solace/podman/graphroot/overlay 2>/dev/null | head
# force it again and WATCH for "Relabeled" lines — silence means nothing matched
restorecon -R -v /app/solace/podman/graphroot
