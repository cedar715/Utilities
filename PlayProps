# Focus on recent activity only (last hour)
(
  increase(sol_event_system_ha_redun_group_node_left_total[1h])
  -
  (increase(sol_event_system_ha_redun_group_node_joined_total[1h]) or 
   on(host) increase(sol_event_system_ha_redun_group_node_left_total[1h]) * 0)
) > 0
