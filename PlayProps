- name: Check directory contents with ls
  ansible.builtin.command: ls -l /path/to/directory
  register: ls_output
  ignore_errors: true

- name: Display directory contents
  debug:
    msg: "{{ ls_output.stdout }}"
