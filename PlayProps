curl -s -u admin:password \
  "http://BROKER:8080/SEMP/v2/monitor/msgVpns/YOUR_VPN_NAME/queues/YOUR_QUEUE_NAME/msgs?count=1&select=spooledTime" \
| jq -r '.data[0].spooledTime' \
| xargs -I{} date -d {} +%s \
| awk -v now="$(date +%s)" '{print now - $1 "s"}'
