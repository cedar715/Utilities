{{ define "nr_subject" -}}
  {{- if gt (len .Alerts.Firing) 0 -}}
    [NEW] {{ (index .Alerts.Firing 0).Labels.alertname }}
  {{- else if gt (len .Alerts.Resolved) 0 -}}
    [RESOLVED] {{ (index .Alerts.Resolved 0).Labels.alertname }}
  {{- else -}}
    [REPEAT] {{ (index .Alerts.Firing 0).Labels.alertname }}
  {{- end -}}
{{- end }}
