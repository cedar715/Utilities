I'm debugging a Solace PubSub+ Enterprise 10.25.0.217 HA deployment using
rootless podman-compose across 3 VMs (primary, backup, monitor — one per VM).

PROBLEM:
- The consul process (Solace's internal redundancy/cluster process) does not
  start on primary or backup.
- "show redundancy" on the monitor does not show HA mode; monitor cannot
  reach primary/backup on the redundancy port (connection refused — nothing
  is listening, so this is a downstream symptom, not a firewall issue).

DIAGNOSIS SO FAR (verified):
1. /sys/fs/cgroup is mounted READ-ONLY inside the container because the
   container runs with cgroupns=host. The broker gets EROFS when creating
   cgroup entries at startup and silently skips spawning consul.
2. Linger is now enabled for the "solace" user and the user slice is active
   with all controllers delegated, so cgroupns=private should now work.

CONSTRAINTS:
- Rootless podman, compose file at /home/solace/solace-compose.yml,
  podman-compose at ~/.local/bin/podman-compose, run as user "solace".
- Multi-VM HA on podman requires host networking (podman bridge networking
  does not support Solace HA redundancy) — do not switch me to bridge.
- Do NOT wipe the storage-group volumes unless you explain why it's required
  (Solace config keys only apply on first boot against empty storage).
- This is a pre-production environment in a regulated bank — prefer
  reversible changes, and keep SELinux enabled if possible (avoid
  label=disable; use proper :Z volume labeling instead).

TASK:
1. Modify my compose file (attached/open) so containers run with
   cgroupns=private and a writable cgroup namespace, compatible with
   podman-compose (note: podman-compose may not support every key — if a
   compose-level option doesn't exist, give me the --podman-run-args
   equivalent or a quadlet/systemd alternative).
2. Give me the exact commands to restart the stack and verify, in order:
   (a) cgroup FS is writable inside the container,
   (b) consul process is running on primary and backup,
   (c) "show redundancy" reports Enabled/Up and the monitor sees both nodes.
3. If consul STILL doesn't start after this, list the next two things to
   check (host cgroups v1 vs v2 via `podman info`, and conmon version
   compatibility with the Solace broker).
