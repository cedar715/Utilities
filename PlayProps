VPN="<VPN>"; Q="<QUEUE>"
NOW=$(date -u +%s)
IN5=$((NOW + 5*60))

curl -su user:pass \
"https://<broker>/SEMP/v2/monitor/msgVpns/$VPN/queues/$Q/msgs?select=msgId,expiryTime&where=expiryTime>=$NOW,expiryTime<=$IN5&count=100"
