# This will group pods by node
for node in $(oc get nodes -o name); do
    echo "Pods running on $node:"
    oc get pods --all-namespaces -o wide --field-selector spec.nodeName=$(echo $node | cut -d'/' -f2)
done
