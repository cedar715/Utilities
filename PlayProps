B = clamp_min(
  sum by (vpn, host) (increase(sol_event_system_cfgsync_down_total[30d]))
- sum by (vpn, host) (increase(sol_event_system_cfgsync_up_total[30d]))
, 0)

(A > 0) or (A == 0 and B > 0 and sum by (vpn, host) (sol_event_system_cfgsync_down_total) > 0)
