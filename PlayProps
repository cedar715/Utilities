- name: Get list of stopped containers (alternative)
  ansible.builtin.command:
    cmd: podman container list --filter status=exited,status=stopped --format "{{.ID}} {{.Names}} {{.Status}}"
  register: stopped_containers
  changed_when: false
