groups:
- name: ha-map
  interval: 1m
  rules:
  - record: ha_pair_host_map
    expr: 1
    labels:
      host: "hk_host"
      ha_pair: "hk-sg-1"
  - record: ha_pair_host_map
    expr: 1
    labels:
      host: "sg_host"
      ha_pair: "hk-sg-1"
  # add more hosts/ha_pairs as needed
