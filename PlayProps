curl -H "Authorization: Bearer $TOKEN" \
  "https://grafana-prod.yourcompany.com/api/v1/provisioning/alert-rules" | \
  jq '[.[] | select(.folderUID == "YOUR_FOLDER_UID")]' \
  > alert-rules-filtered.json
