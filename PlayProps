@echo off
FOR /F "tokens=*" %i IN ('oc get nodes --no-headers -o custom-columns=":metadata.name"') DO (
    echo Pods running on %i:
    oc get pods --all-namespaces -o wide --field-selector spec.nodeName=%i
)
