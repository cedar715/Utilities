loginctl show-user solace -p Linger        # Linger=yes
ls -ld /run/user/$SOLACE_UID               # must exist (linger created it)
runsolace systemctl --user is-system-running   # running/degraded = good; "Failed to connect to bus" = stop
